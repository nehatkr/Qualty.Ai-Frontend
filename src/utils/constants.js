export const BASE_URL= window.location.hostname === "localhost" ? "http://localhost:3000"  :"/api" ;
export const BASE_URL_TEST= window.location.hostname === "localhost" ? "http://localhost:5173"  :"/api" ;
